---
# Product Service Configuration

service:
  name: product-service
  version: 1.0.0
  port: 8080
  
database:
  type: postgresql
  host: localhost
  port: 5432
  name: product_db
  
cache:
  enabled: true
  type: redis
  host: localhost
  port: 6379
  
logging:
  level: info
  format: json
